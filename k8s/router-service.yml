apiVersion: v1
kind: Service

metadata:
  name: router-service

spec:
  selector:
    app: router

  type: ClusterIP

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP