apiVersion: v1
kind: Service
metadata:
  name: ai-service
  labels:
    app: ai
spec:
  type: ClusterIP
  selector:
    app: ai
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP