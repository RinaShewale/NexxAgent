apiVersion: v1
kind: Service


metadata:
  name: auth-service

  labels:
    app: auth



spec:

  selector:
    app: auth



  ports:

  - name: auth-port
    port: 3000
    targetPort: 3000
    protocol: TCP



  type: ClusterIP