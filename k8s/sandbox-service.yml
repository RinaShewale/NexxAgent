apiVersion: v1
kind: Service

metadata:
  name: sandbox-service

spec:
  selector:
    app: sandbox-api

  type: ClusterIP

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP